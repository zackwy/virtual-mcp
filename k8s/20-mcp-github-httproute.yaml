# Exposes the GitHub MCP backend at /mcp/github, alongside the two .well-known
# discovery documents MCP clients fetch before they can start the OAuth flow.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mcp-github
  namespace: agentgateway-system
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: agentgateway-proxy
      namespace: agentgateway-system
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /mcp/github
        - path:
            type: PathPrefix
            value: /.well-known/oauth-protected-resource/mcp/github
        - path:
            type: PathPrefix
            value: /.well-known/oauth-authorization-server/mcp/github
      backendRefs:
        - group: agentgateway.dev
          kind: AgentgatewayBackend
          name: mcp-github
